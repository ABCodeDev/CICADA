// import { fromJS } from 'immutable';
// import { makeSelectEditResponsePageDomain } from '../selectors';

// const selector = makeSelectEditResponsePageDomain();

describe('makeSelectEditResponsePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
