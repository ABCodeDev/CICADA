// import { fromJS } from 'immutable';
// import { makeSelectResponsePageDomain } from '../selectors';

// const selector = makeSelectResponsePageDomain();

describe('makeSelectResponsePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
