// import { fromJS } from 'immutable';
// import { makeSelectNotificationPageDomain } from '../selectors';

// const selector = makeSelectNotificationPageDomain();

describe('makeSelectNotificationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
