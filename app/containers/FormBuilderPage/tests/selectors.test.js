// import { fromJS } from 'immutable';
// import { makeSelectFormBuilderPageDomain } from '../selectors';

// const selector = makeSelectFormBuilderPageDomain();

describe('makeSelectFormBuilderPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
