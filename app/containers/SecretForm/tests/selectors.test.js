// import { fromJS } from 'immutable';
// import { makeSelectSecretFormDomain } from '../selectors';

// const selector = makeSelectSecretFormDomain();

describe('makeSelectSecretFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
