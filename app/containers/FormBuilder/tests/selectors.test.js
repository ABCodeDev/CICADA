// import { fromJS } from 'immutable';
// import { makeSelectFormBuilderDomain } from '../selectors';

// const selector = makeSelectFormBuilderDomain();

describe('makeSelectFormBuilderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
