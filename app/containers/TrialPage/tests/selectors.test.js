// import { fromJS } from 'immutable';
// import { makeSelectTrialPageDomain } from '../selectors';

// const selector = makeSelectTrialPageDomain();

describe('makeSelectTrialPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
